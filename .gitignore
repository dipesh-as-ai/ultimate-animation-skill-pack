node_modules/
reference_repo/
chibi-parts/

# Demo/test files (root level only — NOT references/)
/*.html
wolf.png
chibi.png
ChatGPT*.png

# Keep ALL reference templates tracked
!references/**/*.html

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# CLI bundled assets (regenerated via sync)
cli/assets/
cli/test-project/

# Large media (GIFs in Website UI — tracked via Git LFS or excluded)
references/Website UI/*.gif
